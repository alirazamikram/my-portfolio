declare module "react-scroll" {
  const scroll: any; // You can replace 'any' with a more specific type if available
  const Link: any; // You can replace 'any' with a more specific type if available
  export { scroll, Link };
}
